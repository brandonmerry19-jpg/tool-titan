name: Unpack Tool Titan

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify repository
        run: |
          echo "Tool Titan workflow is running."
          echo "Repository contents:"
          ls -la
